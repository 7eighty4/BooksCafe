// This file is intentionally left blank for now.
// It is loaded by Electron before the renderer process (your React app) starts.
// You can use this script to securely expose Node.js APIs to your app
// by using the `contextBridge` module.
// Example:
// const { contextBridge } = require('electron');
// contextBridge.exposeInMainWorld('myAPI', {
//   doAThing: () => {}
// });
